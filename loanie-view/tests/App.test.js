const data = require('../src/App.js');
const chai = require('chai');

const expect = chai.expect;
const assert = chai.assert;

describe('App', () => {
  describe('loginStat', () => {
    it('should be of type boolean', () => {
      expect()
    })
  })
});